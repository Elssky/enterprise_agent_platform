name: pages-legacy-manual

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow retained for manual fallback. Use optimized-pages.yml for automatic deployment."
